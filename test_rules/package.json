{
  "name": "yoked-church-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Security-rule tests for firestore.rules, run against the Firebase emulator.",
  "scripts": {
    "test": "node rules.test.js"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.0.0"
  }
}
